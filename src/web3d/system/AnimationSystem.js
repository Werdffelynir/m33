
export class AnimationSystem {
    constructor(entityManager) {
        this.entityManager = entityManager;
    }

    update(dt) {
        // знайти всі ентеті з AnimationComponent або mixer
        // викликати mixer.update(dt)
    }
}