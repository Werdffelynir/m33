
export class AISystem {
    constructor(entityManager) {
        this.entityManager = entityManager;
    }

    update(dt) {
        // знайти всі ентеті з AIStateMachine
        // викликати їх update(dt)
        // FSM логіка — всередині компонентів
    }
}
